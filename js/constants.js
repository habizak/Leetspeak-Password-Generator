/**
 * Shared Constants
 * Centralized validation constants to avoid duplicate declarations
 */

export const MIN_LENGTH = 8;
export const MAX_LENGTH = 24;
export const ALPHABET_REGEX = /^[a-zA-Z]+$/;

